{
  "name": "three-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^4.1.3",
    "vite": "^2.3.0"
  },
  "dependencies": {
    "jquerry": "^0.0.1-security",
    "three": "^0.128.0"
  }
}
